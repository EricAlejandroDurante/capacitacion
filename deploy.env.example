# Copiá este archivo a deploy.env (gitignored) y completá con tu host real.
# NO se versiona: el origen del VPS debe quedar privado para no saltar Cloudflare.
#
#   cp deploy.env.example deploy.env
#   # editá deploy.env con tu host
#
VPS_HOST=root@tu-vps-host
